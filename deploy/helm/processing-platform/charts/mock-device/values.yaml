image:
  repository: processing-platform/mock-device
  tag: "dev"
  pullPolicy: IfNotPresent

replicaCount: 1

service:
  port: 8080

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 500m
    memory: 256Mi

env:
  LISTEN_ADDR: ":8080"
  FAIL_RATE: "0"
  LATENCY_MS: "0"
