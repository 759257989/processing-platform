apiVersion: v1
kind: Service
metadata:
  name: worker-realtime
  labels:
    app: worker-realtime
spec:
  type: ClusterIP
  ports:
    - name: metrics
      port: 9090
      targetPort: metrics
  selector:
    app: worker-realtime
