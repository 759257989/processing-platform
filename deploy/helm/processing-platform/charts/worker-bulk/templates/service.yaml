apiVersion: v1
kind: Service
metadata:
  name: worker-bulk
  labels:
    app: worker-bulk
spec:
  type: ClusterIP
  ports:
    - name: metrics
      port: 9090
      targetPort: metrics
  selector:
    app: worker-bulk
