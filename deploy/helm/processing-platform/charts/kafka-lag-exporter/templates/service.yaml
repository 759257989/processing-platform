apiVersion: v1
kind: Service
metadata:
  name: kafka-lag-exporter
  labels:
    app: kafka-lag-exporter
spec:
  type: ClusterIP
  ports:
    - name: metrics
      port: 9090
      targetPort: metrics                             # named port，跟 deployment 对齐
  selector:
    app: kafka-lag-exporter