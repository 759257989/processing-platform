apiVersion: v1
kind: Service
metadata:
  name: devsim
  labels:
    app: devsim
spec:
  type: ClusterIP
  ports:
    - name: metrics
      port: 9090
      targetPort: metrics
  selector:
    app: devsim
    