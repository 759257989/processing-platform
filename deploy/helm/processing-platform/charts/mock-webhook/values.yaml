image:
  repository: processing-platform/mock-webhook
  tag: "dev"
  pullPolicy: IfNotPresent

replicaCount: 1

service:
  port: 8080

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 500m
    memory: 256Mi

env:
  LISTEN_ADDR: ":8080"
