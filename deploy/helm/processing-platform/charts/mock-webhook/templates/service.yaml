apiVersion: v1
kind: Service
metadata:
  name: mock-webhook
  labels:
    app: mock-webhook
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 8080
  selector:
    app: mock-webhook
